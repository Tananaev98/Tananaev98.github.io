ПАМЯТКА: как прорабатывать CSS-оформление игрового HUD под конкретную область
(написано после проработки области "Золотые поля", 2026-08-05 — использовать как чек-лист на каждую следующую область)

Статус по областям: ВСЕ 10 готовы (2026-08-06). --area-accent* нигде не трогали для
областей 1 и 3 (там оно нейтральное, характерный цвет — из --area-tint), для 2/4-10
--area-accent уже тематически подходил и переиспользован для --gp-frame/wood/glow.
1. Смешной лес — мшисто-оливковый (тон из --area-tint, исключение).
2. Золотые поля — золотисто-пшеничный.
3. Обильный край — насыщенно-зелёный (тон из --area-tint, исключение, как у леса).
4. Гиблые воды — тил/синий, панели теплее обычного (холодный оттенок).
5. Беспокойная деревня (переименована 2026-08-06, было "Беспокойный двор") — насыщенный
   медно-терракотовый, отдельный узор area-block (перекрёстный плетень вместо однонаправленной
   штриховки — старый узор был почти неотличим от дефолта). titleAcc: 'Беспокойную деревню'
   (у "деревня" вин. падеж не совпадает с им., в отличие от всех остальных названий областей —
   см. комментарий в lvlData/areas.js, при следующих переименованиях проверять род/склонение).
6. Глухой край — глубокая тёмная зелень.
7. Тихий погост — серо-лиловый, панели теплее обычного (холодный оттенок).
8. Снежное царство — ледяной синий, glow из --area-tint (ярче accent).
9. Огненный хребет — оранжево-красный огонь, glow из --area-tint (ярче accent).
10. Оплот безумия — насыщенный пурпур, единственная область с намеренно более
    насыщенными панелями --gp-panel-a/b (по замыслу самая интенсивная область).

===== Где живут переменные =====
main_css.css, самое начало файла (:root + блоки body[data-area="N"]).
Уже есть --area-accent / --area-accent-mid / --area-accent-dark / --area-accent-darker
(красят карточки меню), --area-tint (мягкая перекраска ОСНОВНОГО фона геймплея,
.main_menu_image, через background-blend-mode:color — яркость/зерно берутся из
исходного пергамента, оттенок из --area-tint, поэтому видимость монстров не страдает)
и --area-bg (сплошной цвет БОКОВОГО фона страницы, body — видно по краям на широких
экранах вокруг игрового поля).

--area-tint и --area-bg — это ДВА РАЗНЫХ визуальных слоя (основной игровой фон и
боковой фон страницы), и у каждой области они должны быть подобраны СВОИ, отдельно
друг от друга — не думать, что один тянет за собой другой.

Для полного HUD-оформления добавлен третий набор переменных с префиксом --gp-*
(gp = gameplay chrome): --gp-frame, --gp-wood, --gp-wood-light, --gp-wood-dark,
--gp-track-dark, --gp-glow, --gp-glow-strong, --gp-panel-a, --gp-panel-b, --gp-subtitle.
Итого три набора, не смешивать: --area-accent* — меню/тайл, --area-tint/--area-bg —
два фона геймплея, --gp-* — HUD-рамки/панели/кнопки боевого экрана.

===== Главное правило =====
:root задаёт значения --gp-*, СТРОГО равные текущим хардкод-хексам (проверять построчно,
не "на глаз"). Это гарантирует, что все области без своего body[data-area="N"] блока
выглядят пиксель-в-пиксель как до правки. Новая палитра добавляется ТОЛЬКО в отдельный
body[data-area="N"] { ... } блок для конкретной прорабатываемой области — ни в коем случае
не менять :root на "новую" тему.

:root — ЧИСТО технический фолбэк на случай отсутствия data-area, а НЕ "дизайн Смешного
леса" (раньше в комментарии было написано иначе — поправлено). У ПЕРВОЙ области тоже
должен быть свой явный body[data-area="1"] блок, как и у всех остальных — не оставлять
её "жить на :root" просто потому что цифры совпадают.

Прорабатывать области по одной, как договорено с пользователем — не каскадировать
на все 10 сразу, даже если технически легко.

===== Что обязательно перекрашивать =====
1. Полоска HP босса (main_css.css, ищи "СТИЛИ ПОЛОСКИ ЗДОРОВЬЯ БОССА"):
   - .boss-health-bar border → --gp-wood, box-shadow glow → --gp-glow
   - .boss-health-ornament-left/-right background → --gp-wood, border → --gp-frame
   - .boss-name text-shadow glow → --gp-glow-strong
2. Панель героя (.hero-status-bar и дети):
   - background-color и диагональный градиент "пергамента" → --gp-panel-a/--gp-panel-b
   - деревянная рамка/outline → --gp-wood / --gp-wood-light
   - .heroImageClass (портрет) border/фон → --gp-wood / --gp-wood-dark
   - .health-bar-container (трек полоски HP героя) → --gp-wood / --gp-track-dark
   - .castle-health-container h3 цвет → --gp-frame
3. Общая "рамка" модалок (используется и приветствием, и победой/поражением,
   и выбором улучшения — это один и тот же .level-up-modal/.modal-content):
   - .modal-content background → --gp-panel-a/--gp-panel-b, border → --gp-frame
   - .modal-content h2 → --gp-frame, .modal-subtitle → --gp-subtitle
   - .upgrade-title → --gp-subtitle, .upgrade-effect → --gp-frame
   - .endgame-stats → --gp-subtitle, .resource-line/.time-line span → --gp-frame
   - .endgame-button (базовый, не цветные варианты) → --gp-frame/--gp-panel-a/--gp-panel-b/--gp-subtitle
   - .endgame-hero-upgrade-title → --gp-subtitle
4. Базовый фон body (крафтовая бумага, самое начало файла) background-color → --area-bg
   (СВОЯ переменная, не --gp-panel-a — панель модалок и боковой фон страницы должны
   уметь отличаться друг от друга по насыщенности).
5. .main_menu_image — background-image: linear-gradient(var(--area-tint), var(--area-tint)), url(...)
   — это ОСНОВНОЙ игровой фон. Для "эталонной"/непроработанной области --area-tint может
   быть transparent, но как только область реально прорабатываешь — обязательно задать
   настоящий полупрозрачный цвет (rgba с alpha ~0.22-0.32, по аналогии с другими областями),
   иначе основной фон останется нейтральным и не будет читаться как "своя область".
6. Проверить @keyframes на хардкод того же хекса, который ты только что вынес в переменную
   (пример: level-up-panel-impact дергал border-color: #8B4513 напрямую — нашлось только
   через grep по хексу ПОСЛЕ правок, не забывать перепроверять).
7. ВАЖНО: game.js местами хардкодит document.body.style.backgroundColor напрямую
   инлайн-стилем (resetGame() ставил '#D1B892' при сбросе, gameOver() ставит '#FF6B6B'
   при поражении — это отдельный игровой эффект "красная вспышка", трогать не нужно).
   Инлайн-стиль ВСЕГДА перебивает background-color из CSS, поэтому если после правки
   --area-bg боковой фон визуально не меняется — первым делом проверить
   document.body.getAttribute('style') на предмет такого хардкода, а не думать, что дело
   в кэше/переменной. Правильный фикс — заменить хардкод на document.body.style.backgroundColor = ''
   (тогда снова работает CSS-правило с var(--area-bg)), а не вписывать туда новый хардкод.

===== Что НЕ трогать (это семантика, не тема области) =====
- .health-bar — зелёный градиент HP героя (зелёный = здоровье, конвенция игры).
- .boss-health-fill / .boss-health-delayed / @keyframes bossHit — красная "урон" и
  жёлто-оранжевая "догоняющая" полоски босса, вспышка попадания — это игровая механика
  (опасность/задержка/отклик на удар), а не оформление области. ПОПРОБОВАЛИ 2026-08-06
  сделать их по теме каждой области (--gp-boss-fill-*/--gp-boss-delayed-*/--gp-hero-hp-*) —
  пользователь посмотрел и явно попросил откатить: "чёт не очень, пусть одинаково будет
  везде, как в первой области". Откат сделан полностью: все 10 body[data-area] блоков
  избавлены от этих переменных, .boss-health-fill/.boss-health-delayed/.health-bar/
  @keyframes bossHit/game.js updateCastleHealthDisplay() вернули на буквальный хардкод,
  каким он был исходно. УРОК: HP-полоски (в отличие от рамок/панелей/кнопок) — это то
  место, где единообразие оказалось важнее тематического разнообразия для этого
  конкретного пользователя — не предлагать красить их по area повторно без явной просьбы.
- .endgame-button.next-level/.restart/.base — у них свой смысл (продолжить/заново/в базу),
  зелёный/оранжевый цвет тут про действие, а не про область.
- .upgrade-option.rarity-* и .upgrade-rarity — цвета редкости прокачки, общие для игры.
- .wound-damage-text/.wound-text и их @keyframes — цвет статуса "ранен", тоже общий.
- .center-text-* (плашки объявлений по центру экрана) — статусные цвета (успех/ошибка/босс).
- .reset-confirm-*/.critical-reset-*/.reset-upgrades-btn — ЦЕЛИКОМ универсальный "опасный"
  диалог (красный/оранжевый, эмблема, кнопка "Да, сбросить"). Специально НЕ перекрашивается
  под область ни в одной детали — предупреждение об удалении прогресса должно выглядеть
  одинаково тревожно на любой area, чтобы не терять узнаваемость "это опасное действие".
- .character-portrait.locked / .locked-message — "недоступный герой", тоже намеренно
  нейтральный/красноватый, как и .area-nav-btn.area-nav-locked — состояние "заблокировано"
  везде в игре выглядит одинаково, не по теме области.
- .close-modal (крестик закрытия) и .character-portrait.selected (золотое кольцо выбора) —
  универсальные UI-акценты (красный крестик, золотая рамка выбора), тоже не по теме области.

===== ВАЖНО (добавлено 2026-08-06): --gp-* — это ЕДИНЫЙ источник для ВСЕХ кнопок и
модальных панелей игры, а не только для боевого экрана =====
main_css.css грузится и в index.html, и в level.html — поэтому --gp-* доступны везде.
Проверено и исправлено на практике: кнопки и модалки МЕНЮ (index_css.css) — "Персонажи",
"Разблокировать всё"/"Сбросить всё" (.select-character-button), кнопки выбора/прокачки
героя (.select-hero-btn/.upgrade-hero-btn), окно секретной разблокировки
(.debug-unlock-dialog + его кнопки/инпут), модалка выбора персонажа
(.character-select-modal .modal-content, .character-portrait, .character-info,
.character-fullimage img, .info-placeholder), чип очков мастерства
(.skill-points-display/-value/-tooltip) — ВСЕ они раньше были захардкожены в старые
#8B4513/#5a3e2b/#3e2a1c/#D1B892/#B89A6D/#5C4033 и не реагировали на область вообще.
Правило на будущее: "тёмная деревянная кнопка" в ЛЮБОМ месте игры (меню или геймплей) —
background: linear-gradient(135deg, var(--gp-wood), var(--gp-wood-dark)),
border: var(--gp-frame) (hover — сдвиг на ступень светлее: var(--gp-wood-light)/var(--gp-wood)).
"Светлая панель" (модалка/чип с текстом поверх) — background: linear-gradient(135deg,
var(--gp-panel-a), var(--gp-panel-b)), border: var(--gp-frame), текст: var(--gp-subtitle),
заголовок: var(--gp-frame). --area-accent* оставлять только для тайлов/карточек уровней
и .area-nav-btn (эти уже были на var() и трогать их не нужно — просто новая область).
При проверке НОВОЙ детали — искать не только основные правила, но и:
(а) @keyframes с тем же хардкодом (дубли уже ловили дважды — в main_css.css и в
index_css.css, у обоих файлов оказалась СВОЯ копия @keyframes level-up-panel-impact);
(б) инлайн style.background/style.color, которые JS ставит НАПРЯМУЮ поверх кнопки —
нашли ещё один в index.html (upgradeBtnInModal.style.background в updateUpgradeButton) —
для "занято/недостаточно золота" состояния было захардкожено то же старое дерево,
хотя "можно повысить" (зелёный) — осталось хардкодом намеренно, это семантика "доступно".

===== ПОПРОБОВАЛИ И ОТКАТИЛИ 2026-08-06: HP-бары (босс/догоняющая/герой) по теме области =====
Кратко для истории: делали через переменные --gp-boss-fill-a/-b(+glow), --gp-boss-delayed-
a/-b/-c/-d(+glow), --gp-hero-hp-1/-2 в :root и всех 10 body[data-area]. Персистентные
предупреждения (lowHealthPulse, тиры HP героя 25-50%/<25%) намеренно оставляли
универсальными, а вот саму заливку и даже мгновенную вспышку попадания (bossHit) в какой-то
момент тоже перекрасили под область по отдельному запросу. После примерки пользователь
решил, что не нравится, и попросил полный откат к единому виду везде — см. запись выше
в "Что НЕ трогать". Переменные удалены из main_css.css целиком (их там больше нет), .css
и game.js правила вернулись на буквальный хардкод. Если тема HP-баров всплывёт снова —
это уже второй раз, когда её пробуют и отменяют; лишний повод сначала спросить/показать
на одной области, прежде чем катить на все десять.

ТЕХНИЧЕСКИ ВАЖНО (осталось актуальным на будущее, даже после отката): CSS-переменные
РАБОТАЮТ внутри инлайн style, который ставит JS — можно просто написать
`el.style.background = 'linear-gradient(to right, var(--x), ...)'` и браузер сам разрешит
var() по текущему cascaded-значению на элементе, никакого getComputedStyle() из JS не
требуется. Проверено и подтверждено в браузере.

Также при этой попытке нашли (и оставили исправленным — это НЕ часть отката, это независимый
баг) ещё один инлайн-хардкод той же природы, что и с body-фоном: game.js
updateCastleHealthDisplay() ставил style.background с хардкодом на КАЖДОЕ обновление
здоровья (не только при resetGame). Раз уже дважды находили такое — при любой новой теме
сразу проверять grep "\.style\.(background|backgroundColor|border|color)\s*=" по game.js
и index.html, не дожидаясь, пока это всплывёт как баг.

===== Как подбирать конкретные хексы для новой области =====
- Для --gp-glow/--gp-glow-strong (свечение полоски босса и имени) брать тот же оттенок,
  что уже выбран для --area-accent этой области в меню — так меню-карточка и боевой экран
  области визуально рифмуются. Пример: Золотые поля --area-accent = #B8860B (goldenrod) →
  --gp-glow: rgba(184, 134, 11, 0.5) — это ровно RGB от B8860B.
  ИСКЛЮЧЕНИЕ: если --area-accent области — просто нейтральный "дефолтный" тон (не несёт
  цветовой идеи самой области), а характерный цвет области на самом деле уже задан через
  --area-tint (как у Смешного леса — accent общий коричневый, а вот тёплый зелёный тон
  давно закреплён в --area-tint) — тогда бери оттенок для --gp-glow ИЗ --area-tint, а не
  из --area-accent. Пример: Смешной лес --area-tint = rgba(90, 150, 80, 0.22) →
  --gp-glow: rgba(90, 150, 80, 0.45) — тот же RGB 90/150/80, просто выше alpha.
- --gp-frame / --gp-wood / --gp-wood-dark — три ступени затемнения одного оттенка
  (используй HSL-подбор на глаз: frame чуть светлее/ярче, wood — основной тёмный, wood-dark —
  почти чёрный того же оттенка). Дока-пример по золотым полям: #7A5209 / #5C4310 / #3D2D08.
- --gp-panel-a/--gp-panel-b — светлая пара для фонов модалок и панели героя, должна читаться
  как пергамент/бумага именно в этом тоне области, не слишком насыщенная (иначе текст поверх
  трудно читать). Проверять контраст --gp-subtitle и --gp-frame поверх --gp-panel-a на глаз.
  ХОЛОДНЫЕ/десатурированные оттенки (синий, тил, серо-лиловый, ледяной) — держать панели
  БЛИЖЕ к тёплому пергаменту, не уводить их полностью в цвет области: холодный цвет большой
  плашкой под тёмным текстом спорит с общим тёплым "пергамент+резное дерево" стилем игры и
  хуже читается. А вот --gp-frame/--gp-wood/--gp-glow (рамки и свечения на ТЁМНОМ фоне полоски
  босса) можно и нужно уводить в полный цвет области — там это смотрится отлично и не мешает
  чтению. Тёплые оттенки (зелёный, коричневый, огненный) можно смело вести в панели полностью,
  они и так близки по духу к пергаменту. Единственное намеренное исключение — Оплот безумия
  (id=10): по замыслу самая интенсивная область, там даже панели специально ведём в полный
  насыщенный пурпур, а не смягчаем.
- --gp-subtitle — текст на светлой панели, должен быть заметно темнее панели, но другого
  оттенка от --gp-frame, чтобы заголовок и подзаголовок не сливались.
- --area-tint (основной игровой фон) — rgba того же оттенка, что и --area-accent области,
  alpha в районе 0.22-0.32 (столько же, сколько у других уже проработанных областей) —
  меньше почти не видно на глаз, больше начинает мешать читаемости спрайтов монстров.
- --area-bg (боковой фон страницы) — сплошной hex, может быть смелее/насыщеннее, чем
  --gp-panel-a (тот должен оставаться помягче ради читаемости текста в модалках) —
  боковой фон это просто цветовое пятно без текста поверх, там насыщенность не мешает.

===== Как проверять в браузере (важные особенности именно этого окружения) =====
- level.html открывается по ?level=N, но у этого файлового пути (file://) браузер-тул
  обрезает query-параметры при навигации — ссылка с ?level= не сработает через navigate().
  Рабочий обход: открыть level.html как есть (уйдёт на уровень 1), затем в консоли
  выполнить `document.body.dataset.area = 'N'` — все --gp-*/--area-accent* переменные
  пересчитаются мгновенно, реальный геймплей/бой можно смотреть без правильной query-строки.
- Обязательно проверить и area="1" (или любую нетронутую) — значения должны совпасть
  с исходными хардкод-хексами один в один (сверять через getComputedStyle, а не на глаз).
- Экран поражения/победы удобно смотреть, просто не трогая прицел — герой сам получит урон
  от врагов и модалка появится сама.
- После правок main_css.css — обязательно поднять версию `?v=...` у main_css.css
  и enemyAlphaMasks.js в level.html (и main_css.css в index.html), иначе изменения
  не подхватятся из кэша. Если правил ещё и game.js — версия у него общая с этими же
  файлами (GAME_BUILD_VERSION), отдельно поднимать не нужно, но проверить, что она
  действительно поднялась в этом заходе.
- Если цвет визуально "не берётся" даже после проверки CSS-переменной через getComputedStyle —
  проверить document.body.getAttribute('style') (и вообще style-атрибут элемента): инлайн-стиль
  из game.js побеждает любой CSS. Не списывать сразу на баг/кэш браузерного инструмента —
  сначала исключить хардкод в JS (см. пункт 7 выше), это оказалось реальной причиной, а не
  особенностью окружения.
- В этом браузер-инструменте вкладки часто переиспользуются (навигация может не делать
  полный свежий reload) — если правка визуально не видна, попробовать navigate на ДРУГУЮ
  страницу и обратно перед тем, как искать баг в коде.
- Отдельно от предыдущего пункта: getComputedStyle(...).borderColor конкретно иногда
  показывает СТАРОЕ значение сразу после смены document.body.dataset.area, даже когда
  сама переменная (--gp-frame и т.д.) уже точно обновилась и другие свойства (background)
  уже правильные — проверено много раз. Просто `void el.offsetHeight` не всегда помогает
  именно для border-color; надёжно помогает `el.style.display='none'; void el.offsetHeight;
  el.style.display='';` на самом элементе. Это артефакт браузерной панели инструмента,
  не баг вёрстки — не тратить время на поиск несуществующей причины в CSS, если background
  и остальные свойства уже показывают верное значение.

===== Порядок действий на новую область =====
1. Прочитать текущий блок body[data-area="N"] с --area-accent*/--area-tint — оттуда брать
   базовый тон (если у области ещё нет собственного блока — смотреть, что уже решено для
   неё раньше в других местах проекта, например --area-tint из прошлой сессии по фону).
2. Добавить (или дополнить) body[data-area="N"] блок с --area-tint/--area-bg/--gp-* по
   формуле выше. --area-accent* трогать только если меню-карточки этой области ещё не
   были отдельно утверждены пользователем — иначе не менять, чтобы не сдвинуть уже
   одобренный экран меню.
3. Проверить в браузере: welcome-экран, полоску HP героя, полоску HP босса (спровоцировать
   бой или форсировать класс .show + вписать текст/ширину вручную через консоль — быстрее,
   чем ждать реального спавна), экран победы/поражения.
4. Проверить через getComputedStyle, что: (а) соседняя уже проработанная область не
   изменилась, (б) непроработанная область по-прежнему падает на чистый фолбэк :root
   (те же хардкод-хексы, что были всегда).
5. Показать результат пользователю, дождаться подтверждения, только потом переходить
   к следующей области.
