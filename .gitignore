Thumbs.db
desktop.ini
.DS_Store
tmp/
DataExport/
node_modules/

# Черновые рабочие папки генерации/обработки картинок (raw/eroded/keyed/final
# кадры, PowerShell/Python-скрипты для конкретного уровня) — промежуточные шаги
# на пути к готовым .webp в images/, самой игре не нужны. Шаблон, а не список
# конкретных папок — под каждый новый уровень появляется своя (.codex-levelNN,
# .codex-levelsNN-MM, .codex-redraw-NN-MM и т.п.).
.codex-*/

# Случайно созданная пустая папка без содержимого и назначения.
Новая папка/
